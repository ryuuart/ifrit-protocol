# C/C++
.cache/clangd/
build/
build-*/
CMakeUserPresets.json
dist/

## The binary schema a Python sketch reads its own messages through. It
## is written beside the entry because that is where the sketch reaches
## it, and the build writes it from the .fbs every time.
src/sketch/sketches/*/*.bfbs

## Qt
.qmlls.ini
