"""Fixtures for the reference generator's pure parts.

Run them with `python3 -m unittest discover -s scripts/sigil/reference/test`
from the application root, or through ctest, which registers one entry
for the whole directory.
"""
