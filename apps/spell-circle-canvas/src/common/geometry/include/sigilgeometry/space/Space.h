#pragma once

/** @file
 * TRANSITIONAL. The one space scope split into the camera and the
 * renderer, both under the mesh tier; this is its former path. Include
 * <sigilgeometry/mesh/camera/Camera.h> and
 * <sigilgeometry/mesh/render/Painter.h> instead, and delete this file
 * once no caller reaches for the old spelling.
 */

#include "sigilgeometry/mesh/camera/Camera.h"
#include "sigilgeometry/mesh/render/Painter.h"
