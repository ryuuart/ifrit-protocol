#pragma once

/** @file
 * TRANSITIONAL. Model interchange moved under the mesh tier; this is
 * its former path. Include <sigilgeometry/mesh/codec/Model.h> instead,
 * and delete this file once no caller reaches for the old spelling.
 */

#include "sigilgeometry/mesh/codec/Model.h"
