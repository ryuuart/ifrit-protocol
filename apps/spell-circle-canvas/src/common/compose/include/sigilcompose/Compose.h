#pragma once

/** @file
 * SigilCompose — the transitional root umbrella: the kernel, which is what
 * this header has always meant. Each feature has an umbrella of its own
 * under its directory, and each header under a feature stands on its own.
 */

#include "sigilcompose/core/Core.h"
