#pragma once
// The instances binary's support: the kernel harness plus the instanced leaf
// and the kit's placers, which are everything its tests exercise.

#include <sigilcompose/instances/Instances.h>
#include <sigilcompose/kit/Placers.h>

#include "CoreTestSupport.h"
